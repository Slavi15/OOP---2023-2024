#include "Parser.h"

int main()
{
	/*
	
	char buffer[MAX_TEXT_LENGTH + 1];
	std::cin.getline(buffer, MAX_TEXT_LENGTH + 1);

	Table table;
	Parser parser(buffer, table);

	parser.readFromFile();

	parser.getInput();

	parser.writeToFile();

	*/

	return 0;
}